#pragma once
#ifndef MENU_H
#define MENU_H
#include <string>
using namespace std;

extern int operator ~(string);
extern int menu(string, string, string, string);
extern int menu(string, string, string, string, string);

#endif